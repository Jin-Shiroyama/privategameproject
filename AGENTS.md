# AGENTS.md

## プロジェクトと適用範囲

CUI・チャットベースの放置型「関係性観察ゲーム」。キャラ達の会話ログを眺め、関係性が遷移する瞬間を楽しむ。常駐型で、アプリ終了・一時停止・PCスリープ中は世界も止まる。

このファイルはリポジトリ直下に置き、リポジトリ全体の作業指針とする。

## 設計の正本と判断の進め方

- 設計の正本は `docs/design_v6.md`。作業前に正本の確度表記、§12の実装段階、および変更対象の節を読む。正本が見つからない場合は、その内容を推測して実装せず配置を確認する。
- 「確定」「実装案」「検証用案」「未決・候補」を区別する。数値例や擬似YAMLを確定済みのバランス値・正式スキーマと見なさない。
- 技術方針は、提供済みの `CLAUDE.md` に合わせてPython 3.12+を採用する。正本§13では言語が未決として残っているが、本作業指針ではPythonによる試作を前提とする。C++移植までは決定済みと扱わない。
- 正本と矛盾する変更、新しいゲーム仕様の決定、未決事項の採用が必要な場合は、理由・影響・代替案を示してユーザーに判断を求める。影響しない範囲の作業は進めてよい。
- 既存方針内の命名、関数分割、局所的な不具合修正など、ゲーム仕様を変えない通常の実装判断は自律的に進める。
- 別AIの提案も正本と照合して妥当性を検討する。提案されたこと自体を採用の根拠にしない。
- `CLAUDE.md` と本ファイルの共通方針を維持する。新たな矛盾を見つけたら黙って片方を採用しない。仕様変更の承認後は、必要な正本・作業指針も更新して決定理由を残す。

## 現在のスコープ：第①段階のみ（正本§12）

実装するもの：

- 通常交流、恋慕、告白の受諾・拒否、クールダウン後の再告白、テンプレートログ。
- solo属性から導出する相性の最小関数（§21）。
- EventResult、参加者の役割、日時、定義版、安定した確定順の記録。
- 観測者候補、fact、factごとの開示範囲、当事者・目撃者のknowledge参照の記録（§19）。
- 終了時保存・定期自動保存とスナップショットからの再開（§8）。

初期の検証用構成はキャラ2〜3人、性格タグ数個、好感度・友情度・恋愛度を基本とする。有効値の参照窓口を用意し、cap未実装の間は保存値をそのまま返す。

後段へ送るもの：

- 第②段階：スキル効果・「一途」・複数交際の機能検証、ReactionRule評価、噂話、知識条件・認識ビュー。
- 第③段階：降格・解消、悪化の持続判定、関係保護・保存値下限。
- 第④段階：初体験の自動検出、経験集計、経験からのスキル習得、有効値上限cap。
- 別途の接続課題：LLM接続、ChildEntity・成人変換、人口運用、本格的な追加コンテンツ。

後段のための型・参照窓口・記録項目は最小限に予約してよい。ただし予約を理由に機能本体や汎用フレームワークを先行実装しない。ペアをまたぐ単一恋人制約など、将来の複数交際を妨げる制約も追加しない。

## 守るべき設計境界

- シミュレーション層とテキスト化層を分離する。シミュレーションはLLMなしで成立し、描写側は確定済みの結果を変更しない（§2）。
- solo、方向付きのdirected、ペア共有のpairを分離する。A→BとB→Aは独立。面識・友情・恋愛・固定タグを混同しない（§3・§6）。
- 感情軸は独立。好感度や友情を全感情・全告白の共通前提にしない。恋愛度が高いだけで恋人状態へ変更しない（§4・§16）。
- 告白の発生は抽選、返答は結果適用前の情報による決定的な条件判定。成功時deltaで成功条件を満たしたことにしない。拒否後の恋愛度を自動でゼロにせず、再告白に状況改善を一律要求しない（§16）。
- latent軸・生数値・相性値をLLMプロンプト用データに含めない境界を保つ。expressedは有効値の帯とhintへ変換する。神視点の表示とキャラが知る情報を分ける（§4・§19・§21）。
- factはEventResultの開示可能な成立結果からのみ生成し、内部deltaや軸値をfact化しない。`observed_by`は観測者候補であり、全結果の開示先ではない。第①段階ではfact・開示先・knowledgeを元イベントと一括記録する（§19・§20）。
- 相性はsolo由来の導出値であり、直接deltaで変更しない。同じ相性ルールは1方向につき最大1回適用し、対称条件が両方一致しても二重加算しない（§21）。
- 通常のCasterは全員成人。コアはSFWで完結させ、NSFWコンテンツはパックに分離する。性愛度と受諾を同一視しない（§10）。
- ChildEntityを導入する際はCasterと別型にし、恋愛・性愛系フィールドを持たせない。恋愛・NSFWイベント、反応の役割、NSFW fact・プロンプトへの混入をエンジン側で拒否し、設定で解除可能にしない。初期段階でChildEntity本体を実装する必要はないが、この分離を妨げる型設計をしない（§11）。

## 技術方針

- Python 3.12+。型ヒントを付け、mypy strictまたはpyright strictを通す。既存設定があればそれに合わせる。dataclassを中心とし、依存は最小限にする（PyYAML、pytest、ruff、mypy程度）。
- 軸・トラック・イベント・相性ルールはYAMLの定義データで管理する。ゲーム固有の条件・数値をエンジンに散在させない。§11の不変条件は可変ルールから分離する。
- YAMLは安全なローダーで読み、型・必須項目・参照IDを検証する。不正な定義を黙って補正しない。
- 乱数は注入された単一の `random.Random` インスタンス経由のみ。シミュレーション内部でグローバルな乱数関数や `datetime.now()` を使わない。ゲーム内時刻はtick由来とし、実時間の計測は外側の進行制御で扱う。
- 乱数を消費してよいのはシミュレーション層のみ。テキスト化・表示は乱数を消費せず、EventResultの整数連番IDから `result_id % バリエーション数` で決定的に選択する。組込み `hash()` は文字列に対してプロセス毎に値が変わり得るため使用しない。再描写・保存再開後の過去ログ再生成がシミュレーションの乱数系列に影響してはならない。
- 同一seed・同一入力・同一定義版・同一実行条件で再現できること。候補列挙・同時刻の処理順・ID採番も安定させ、集合の列挙順や実時間に依存させない。
- tickはスケジューラ。deltaはイベント単位または経過ゲーム内時間単位で定義し、描画頻度やtick頻度によって感情の増減量を変えない。停止時間を復帰時にまとめて進めない（§8）。
- 実時間→tickの変換はループ毎に `elapsed = min(実測経過, 上限)` でクランプする（§8）。復帰時は最大で上限分だけ進み、高負荷で上限を超えた分は捨てる。上限値は定義データ側の設定。明示的な一時停止・終了後の再起動では計測基準を置き直し、クランプで代替しない。
- 抽選候補は（イベント定義ID, 正規化した参加者タプル）で重複排除し、同じキーでソートしてから重み付き抽選する。方向付きイベントは役割順タプル、ペア共有イベントはソート済みタプルで正規化する。
- §7の順序を守る：開始時情報の取得・候補除外と抽選 → 結果候補作成 → 増減補正 → 仮更新 → 状態更新 → 一括確定 → テキスト化。未実装段の処理を空けても、責務の境界は保つ。
- 同じイベントの無限再評価・即時連鎖・二重適用を防ぐ。今回成立した結果と、後から起き得るイベント候補を分ける。
- セーブには世界状態、ゲーム内時刻、RNG状態、クールダウン、定義版、順序・ID、実装済みの未処理作業を含める。確定単位の途中を保存しない。
- 保存先と同じファイルシステムの一時ファイルへ書込み完了後、アトミックに差し替える。論理的な一括確定とファイル差し替えは両方必要。読込み失敗を初期化で隠したり、既存セーブを上書きしたりしない。

## 作業・検証・報告

- 開始時に既存コード、README、依存・型検査・テスト設定、作業中の差分を確認する。ユーザーや他エージェントの変更を取り消さない。依頼に無関係な改修を広げない。
- 機能変更は、現在の段階に対応する受け入れ条件をpytestで検証する。第①段階では§12の1・2・11・12の通常再告白部分と、§22の①記録・時間／①相性を対象にする。全17件の完成を第①段階へ持ち込まない。
- 決定論の主検証は、同一seedの連続実行と途中保存・再開後の状態／イベント列／ログの一致とする（テンプレート内容・並び順・定義版が同一という前提を付ける）。グローバル乱数関数などの使用を検索する検査は補助に留める。告白判定の境界、開示先の分離、内部deltaの非fact化、相性の二重加算防止も確認する。
- リポジトリの既存コマンドを優先してテスト・lint・型検査を実行する。未設定なら採用構成に合う実行方法をREADMEへ記載する。未実行・失敗した検証は理由を報告し、成功したと扱わない。
- コミットする場合は小さく、関連する検証が通る単位にする。日本語メッセージ可。APIキー・認証情報・個人用セーブをコミットしない。
- 依頼されたマイルストーンの完了時に成果と検証結果を提示してレビューを求める。すでに継続の指示がある場合を除き、次のマイルストーン・実装段階へ勝手に進まない。
- 応答・コメント・docstringは日本語。報告は変更内容、検証結果、残る問題を簡潔に示す。仕様判断が必要な場合は具体的な論点を示し、不要な新提案で締めくくらない。
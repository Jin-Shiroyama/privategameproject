# CLAUDE.md

## プロジェクト
CUI・チャットベースの放置型「関係性観察ゲーム」。キャラ達の会話ログが流れ続けるのを眺める。関係性の遷移が主軸。常駐型(アプリを閉じれば世界も止まる)。

**設計の正本は `docs/design_v6.md`**。実装判断はすべて正本に従うこと。正本と矛盾する実装・正本にない仕様判断が必要になったら、実装せず質問すること。

## 現在のスコープ: 第①段階のみ(正本§12)
- 実装する: 通常交流 / 恋慕 / 告白の受諾・拒否(決定的な条件判定) / クールダウン後の再告白 / テンプレートログ / 相性最小関数(§21) / EventResult・fact・開示範囲・knowledge参照の記録(§19) / スナップショット保存(§8)
- 実装しない(型・接続点の予約のみ可): LLM接続 / 有効値上限cap / 関係保護(§17) / ReactionRule評価(§20) / ChildEntity(§11) / 経験集計・スキル習得(§14) / 噂話
- cap未実装の間、有効値の参照窓口は保存値をそのまま返す(§4)

## 不変条件(スコープ外でも構造で守る)
- §11: ChildEntityに恋愛・性愛系フィールドを型として持たせない。NSFW系へのChildEntity混入をエンジンレベルで拒否できる型設計にする
- latent軸・生数値・相性値をLLMプロンプト用データに含めない境界を保つ(§4・§21)
- 知識の源はEventResultのみ。delta・軸値をfact化しない(§19)

## 技術方針
- Python 3.12+、型ヒント必須(mypy strict または pyright strict を通す)
- 依存は最小(pyyaml, pytest, ruff, mypy程度)。dataclass中心
- 軸・トラック・イベント・相性ルールは定義データ(YAML)。コードにハードコードしない(§4)
- **決定論**: 乱数は注入された単一のRandomインスタンス経由のみ。`random`のグローバル関数・`datetime.now()`をシミュレーション内で使わない(ゲーム内時刻はtick由来)。同一seed+同一入力で全実行が再現できること
- §7のイベント処理順(候補除外→結果作成→補正→仮更新→状態更新→一括確定→テキスト化)と一括確定の単位を崩さない
- 保存は一時ファイル書き込み→アトミック差し替え(§8)。RNG状態・ゲーム内時刻・未処理作業を含める

## 作法
- テスト: 受け入れシナリオ(正本§12)をpytestで表現し、常に緑を維持
- コミットは小さく、テストが通る単位で。メッセージは日本語可
- マイルストーン完了ごとに停止してレビューを求める(勝手に次へ進まない)
- 応答・コメント・docstringは日本語